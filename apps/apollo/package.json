{
  "name": "apollo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node-dev --transpile-only --no-notify --exit-child index.ts",
    "test": "jest",
    "test:watch": "jest --watch",
    "codegen": "graphql-codegen --config codegen.ts"
  },
  "dependencies": {
    "@websockets/utils": "workspace:*",
    "apollo-server": "^3.13.0",
    "graphql": "^16.9.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.3"
  }
}
